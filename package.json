{
  "name": "plobi-kit",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute DB --local --file=db/schema.sql",
    "db:remote": "wrangler d1 execute DB --remote --file=db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
